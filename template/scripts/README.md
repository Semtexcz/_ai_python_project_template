# Scripts

Store project helper scripts here.

Scripts may automate local setup, validation, maintenance, or release tasks. Keep primary application logic in `src/`.

Run `./scripts/check.sh` or `make check` for the default local quality checks.
