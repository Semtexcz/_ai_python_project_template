# Prompts

Store reusable AI-agent prompts and prompt templates here when they are specific to this project.

Prefer prompts that reference repository artifacts such as `AGENTS.md`, `docs/`, `product/`, and `project/` instead of duplicating long context.

Start with `implement-task.md` when asking an AI coding agent to implement a scoped backlog task.
